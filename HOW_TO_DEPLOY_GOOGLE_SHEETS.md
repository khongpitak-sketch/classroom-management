# คู่มือการติดตั้ง Google Apps Script และเชื่อมต่อ Google Sheets เป็นฐานข้อมูล

ระบบจัดการห้องเรียนรองรับการใช้ **Google Sheets** เป็นฐานข้อมูลจริงบนคลาวด์ บันทึกข้อมูลห้องเรียน (ชั้น 3, 4 และแล็บคอม 1-4), ตารางเรียน, การจองห้อง และการแจ้งซ่อมแบบเรียลไทม์

---

## 📋 ขั้นตอนที่ 1: สร้าง Google Sheets และเปิด Apps Script

1. เข้าไปที่ [Google Sheets](https://sheets.new) เพื่อสร้างสเปรดชีตใหม่
2. ตั้งชื่อไฟล์สเปรดชีต เช่น `ระบบจัดการห้องเรียน - Database`
3. ไปที่เมนูด้านบน คลิก **ส่วนขยาย (Extensions)** ➔ **Apps Script**

---

## 📝 ขั้นตอนที่ 2: วางโค้ด `Code.gs` และ `Index.html`

### 1. วางโค้ดในไฟล์ `Code.gs`:
- ลบโค้ดเดิมในแท็บ `Code.gs` ออกทั้งหมด
- เปิดไฟล์ [`Code.gs`](file:///C:/Users/PNUCOM4_043/.gemini/antigravity/scratch/classroom-management-system/Code.gs) ในโปรเจกต์นี้ คัดลอกโค้ดทั้งหมดไปวางใน `Code.gs` บนหน้าต่าง Apps Script
- กดปุ่ม **บันทึกโครงการ (Save project)** (ไอคอนแผ่นดิสก์)

### 2. สร้างไฟล์ `Index.html`:
- ในหน้าต่าง Apps Script ด้านซ้าย คลิกเครื่องหมายบวก **(+)** ข้างคำว่า "ไฟล์" (Files) ➔ เลือก **HTML**
- ตั้งชื่อไฟล์ว่า `Index` (ระบบจะใส่เป็น `Index.html` ให้อัตโนมัติ)
- ลบโค้ดเริ่มต้นออก แล้วคัดลอกโค้ดทั้งหมดจากไฟล์ [`Index.html`](file:///C:/Users/PNUCOM4_043/.gemini/antigravity/scratch/classroom-management-system/Index.html) ไปวาง
- กดปุ่ม **บันทึกโครงการ (Save)**

---

## ⚙️ ขั้นตอนที่ 3: สั่งสร้างฐานข้อมูลเริ่มต้น (Initial Setup)

1. ในหน้าต่าง Apps Script ที่แท็บ `Code.gs`
2. บริเวณเมนูแถบด้านบน มองหาแถบดรอปดาวน์เลือกฟังก์ชัน (ข้างปุ่มเรียกใช้ / Run)
3. เลือกฟังก์ชัน **`setupDatabase`**
4. คลิกปุ่ม **เรียกใช้ (Run)**
5. ในการรันครั้งแรก Google จะขอสิทธิ์เข้าถึง (Authorization):
   - คลิก **ตรวจสอบสิทธิ์ (Review Permissions)**
   - เลือกบัญชี Google ของคุณ
   - คลิก **ขั้นสูง (Advanced)** ➔ คลิก **ไปที่ ระบบจัดการห้องเรียน (ไม่ปลอดภัย)**
   - คลิก **อนุญาต (Allow)**
6. เมื่อรันสำเร็จ กลับไปดูที่ Google Sheets จะเห็นแผ่นงาน 4 แผ่นถูกสร้างและลงข้อมูลอัตโนมัติ:
   - 📊 **Rooms**: ห้อง 302, 303, 305, 306, 307, 308, 401, 402, 403, 404, 405, 406, ห้องปฏิบัติการคอม 1, 2, 3, 4
   - 📅 **Timetable**: ตารางการใช้ห้องเรียน
   - 📝 **Bookings**: รายการจองห้องเรียน
   - 🔧 **Maintenance**: รายการแจ้งซ่อมอุปกรณ์

---

## 🚀 ขั้นตอนที่ 4: เผยแพร่เป็นเว็บแอปพลิเคชัน (Deploy Web App)

1. คลิกปุ่มสีน้ำเงินด้านขวาบน **การทำให้ใช้งานได้ (Deploy)** ➔ **การทำให้ใช้งานได้รายการใหม่ (New deployment)**
2. คลิกไอคอนรูปเฟือง **เลือกประเภท (Select type)** ➔ เลือก **เว็บแอป (Web app)**
3. กรอกข้อมูลการ Deploy:
   - **คำอธิบาย (Description)**: `Classroom Management System v1`
   - **ดำเนินการในฐานะ (Execute as)**: `ฉัน (Me - your-email@gmail.com)`
   - **ผู้มีสิทธิ์เข้าถึง (Who has access)**: **`ทุกคน (Anyone)`** *(สำคัญมาก เพื่อให้หน้าเว็บภายนอกหรือ GitHub Pages ส่งข้อมูลมาบันทึกได้)*
4. คลิกปุ่ม **ทำให้ใช้งานได้ (Deploy)**
5. คัดลอก **URL เว็บแอป (Web app URL)** (รูปแบบ: `https://script.google.com/macros/s/AKfycb.../exec`)

---

## 🔗 ขั้นตอนที่ 5: นำ URL ไปเชื่อมต่อในหน้าเว็บ (หรือ GitHub Pages)

1. เปิดหน้าเว็บระบบจัดการห้องเรียน (บนเครื่องหรือบน GitHub Pages)
2. ไปที่แท็บ **จัดการข้อมูล (Settings)**
3. ในกล่องสีเขียว **"เชื่อมต่อฐานข้อมูล Google Sheets (Google Apps Script API)"**
4. วาง URL ที่คัดลอกมาลงในช่องข้อความ
5. คลิกปุ่ม **"เชื่อมต่อ"**
6. ป้ายสถานะจะเปลี่ยนเป็น `🟢 เชื่อมต่อ Google Sheets แล้ว` ข้อมูลห้องเรียนและการจองจะซิงก์เข้าสู่ Google Sheets อัตโนมัติทันที!
